“ 设置中文帮助文档
set helplang=cn
set encoding=utf-8

" 映射ctrl + u使当前光标下得单词变为大写，并移动光标到单词尾
nnoremap <c-u> viwUe
inoremap <c-u> <esc>viwUe

" 设置mapleader为","，默认为反斜杠
let mapleader=","

" 映射<leader>ev，纵向分屏打开vimrc文件
nnoremap <leader>ev :vsplit $MYVIMRC<cr>
" 映射<leader>sv, 使vimrc立即生效
nnoremap <leader>sv :source $MYVIMRC<cr>

" iabbrev类似映射，在插入模式输入tjl，会替换为后面得字符串，iabbrev在单词纠错方面很有用。
iabbrev tjl # ---<cr># TianJingLiang<cr># tianjingliang@qq.com
